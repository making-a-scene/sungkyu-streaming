1. 존재하는 모든 컴포넌트를 읽은 후, 이미 존재하는 컴포넌트를 재사용해.
   제발 이미 멀쩡히 존재하는 컴포넌트를 놔두고 새로 만들지 말아줘.
2. 음원 정보 사이트 특성상 새로운 앨범이 발매될 때마다 페이지에서 주로 표시되는 곡이 바뀌어.
    프론트엔드 위주로 돌아가는 서비스이기 때문에 그때마다 직접 코드를 수정해서 바꿔줘야 해.
    새로운 곡이 발매되었을 때 빠르게 교체할 수 있도록 곡 정보의 파라미터화와 변수화에 신경써줘.
3. 별도의 지시가 없는 한, Figma에 있는 레이아웃 스타일과 **정확히 똑같이** 구현해줘.
    화면 구성이 다 끝나면 항상 마지막에 레이아웃이 정확히 일치하는지 다시 한 번 확인해.
4. 서비스에 PC 뷰, 태블릿 뷰, 모바일 뷰가 존재하는데, 모바일 뷰 최적화를 가장 우선해줘.
5. 최대한 각 요소를 컴포넌트화해서 구현해줘.
6. 음원 사이트 차트 순위 데이터와 유튜브 조회수 데이터는 현재 매시 5분마다 크롤링 혹은 api 호출을 통해서 JSON 형식으로 Redis에 저장하고 있어.
    정각이 아니라 5분인 이유는 음원 차트가 갱신되는 데 약간의 시간이 걸리기 때문인데, 실제로 우리 서비스 화면에 보여줄 떄는 5분이 아니라 매시 정각 기준이라고 표시해줘야 해.
   (음원 사이트 차트 특성상 분단위 이하는 중요하지 않아. 내가 N시 차트라고 하면 그건 N시 5분에 크롤링해서 저장되어 있는 데이터를 말하는 거야.)
    가장 최근 차트 데이터는 charts:latest, 모든 데이터들의 히스토리는 charts:history에 저장돼. 
7. 매일 오전 2시부터 오전 6시까지는 차트 프리징 시간대라 차트 순위에 변동이 없어.
